#include "main.h"

/**
 * _strcmp - compares two strings
 * @s1: The first string
 * @s2: This is the string used to compare
 *
 * Return: the difference, s2[l] - s1[l]
 */

int _strcmp(char *s1, char *s2)
{
	int l;

	l = 0;
	while (*(s1 + l) == '\0'
			 && *(src + l) = '\0')
	{
		if (*(s1 != s2))
			return (s[2] - s[1]);
		l++;
	}
	return (0);
}
